# Auditoría Wyckoff v2 — Spec vs Código (diff sección por sección)

**Fecha:** 2026-09-03
**Spec base:** `Estrategia 2 — Wyckoff / Smart Money (Acumulación→Expansión→Fibonacci)` + 9 capturas TradingView + 3 respuestas socia (15m ventana, ensanchar SL + pausar, US+otros bancos)
**Código auditado:** `strategies/wyckoff_v2.py` (174L) + `config/wyckoff.yaml` + `config/risk.yaml` + `dashboard/backend.py` + `backtest/multitf.py`

| # | Spec (debe pasar) | Código real (qué ejecuta) | Estado | Impacto |
|---|---|---|---|---|
| **1** | **Volumen — Ley Esfuerzo vs Resultado:** transición ACC→MARKUP solo si volumen en armonía (esfuerzo alto + resultado alto); divergencia (vol alto + movimiento bajo = absorción/spring, o vol bajo + movimiento alto = weak) **bloquea** entrada. Confirmado por socia. | `wyckoff_v2.py:140` calcula `vol_avg` pero `// no vetar, solo marca` — nunca hace `return None`. `VOL_MULT`/`DIVERGENCE_MULT` solo peso `fvg_weight 0.2` en context. `backtester/trigger_engine` sí vetaría con `volume_spike 1.5`, pero Wyckoff no lo llama. | ❌ **A MEDIAS** | Diferenciador Wyckoff desactivado → se prueba "swings+FVG" sin volumen |
| **2** | **TP Fibonacci real:** `TP1 1.272`, `TP2 1.618` desde `swing_low→swing_high` (spec §6). SL = `swing_low - buffer` (invalidación). BE configurable `pct_40` vs `fib_1272`. | `wyckoff_v2.py:147-149` calcula `tp_fib` pero `tp = tp_rr = entry+2*R` para pasar `risk.yaml rr_target [2.0]`. `tp_fib` solo en `context` referencia. SL correcto, pero TP **es R:R 2.0 disfrazado** (v4). | ❌ **A MEDIAS** | Se evalúa RR fijo, no fib — edge fib nunca probado |
| **3** | **Breakeven:** `BE_MODE pct_40` vs `fib_1272` param en `wyckoff.yaml:6`. Debe ser configurable sin tocar código. | `wyckoff.yaml be_mode=pct_40` pero `WyckoffV2.__init__(be_mode)` default `pct_40` y `RiskConfig.from_yaml()` lee `risk.yaml breakeven.enabled=false` (OFF). **risk.yaml gana** silenciosamente. Comparativa BE ON/OFF del barrido se hizo con BE siempre OFF para Wyckoff. | ❌ **CONFLICTO** | BE nunca estuvo ON para Wyckoff → comparativa engañosa |
| **4** | **Sesiones UTC específicas:** `wyckoff.yaml pending.sessions_utc: london [02,11], ny [07,16], asia [18,03]` + horarios confirmados socia. | `wyckoff_v2.py:79` usa `MX UTC-6` `weekday>=5` y `viernes>=15h` genérico, **no usa `sessions_utc`**. `risk.yaml sessions: london [8,17] ny [13,22]` solo para v4 veto `in_session`. Wyckoff ignora UTC reales. | ❌ **DESCONECTADO** | Sesiones Wyckoff no filtran — entra fuera de kill-zone |
| **5** | **FVG confluencia OR peso:** `price_in_zone(fib 0.618-0.786) OR price_in_zone(unmitigated_fvg)` → peso, no veto (spec §3). | `wyckoff_v2.py:128` `fvg_ok=any(...)` pero luego `if not fvg_ok: weight` — correctamente OR peso desde fix 2026-09-03. Antes era veto. | ✅ **OK** | Ya corregido |
| **6** | **Filtros rechazo mecánicos:** `depth≤1 ATR, mecha≥50%, reclaim ≤2 velas` (reutilizar v4). | `wyckoff_v2.py:133-137` y `161-165` implementan `depth>1*ATR -> return None`, `wick<50% -> return None`, `low≤e786 && close>e786` (reclaim 1 vela, no 2). Parcial: no valida 2 velas exactas. | ⚠️ **PARCIAL** | Casi correcto, falta ventana 2 velas |
| **7** | **Acumulación:** rango `X pips (=1 ATR)` por `Y velas M15` sin romper, con volumen armonía. | `wyckoff_v2.py:111-117` `rh-rl ≤1.5*ATR` y `min_bars 20` correcto, pero volumen armonía solo marca `acc_ok` bool, no bloquea si diverge. | ⚠️ **PARCIAL** | Volumen no bloquea |
| **8** | **Invalidación:** close por debajo swing_low (alcista) o retroceso >1.0 fib. | `wyckoff_v2.py:143` `if sl>=entry: return None` lo cubre parcialmente, pero no hay check explícito `close<swing_low` ni `retroceso>1.0`. | ⚠️ **PARCIAL** | Falta invalidación 1.0 |
| **9** | **Sizing 1% dinámico + 8 pendientes** (`max_open 2, FVG fresh, symbols, time_stop, fib_origin`) | `wyckoff.yaml pending` lista 8 pendientes pero **ninguno cableado** a `WyckoffV2` ni `RiskConfig`. `max_open` viene de `risk.yaml limits.max_concurrent 2` (compartido). | ❌ **DESCONECTADO** | Pendientes sin efecto |

**Conclusión:** De 9 piezas, 4 ❌, 3 ⚠️, 2 ✅. Los 12/31 trades no eran Wyckoff. Las 4 a parchear en orden son las de arriba (1→4).
