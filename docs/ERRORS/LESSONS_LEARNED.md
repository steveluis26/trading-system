# LESSONS_LEARNED
- pip 1.0 vs 100 (100×) — usar pip_value_usd_per_standard_lot, test debe leer de yaml
- ATR a medias — grep ATR debe dar 0 en código activo, no solo comentarios
- vol peso vs veto — si no hay if not vol_ok: return None, no es veto
